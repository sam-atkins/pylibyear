[tool.poetry]
name = "example-poetry-project"
version = "0.0.0"

[tool.poetry.dependencies]
python = "^3.12"
flask = "^3.0.0"
flask-restx = "^1.0.5"


[tool.poetry.dev-dependencies]
autoflake = "^1.3"
black = "^24.0.0"
coverage = "^7.4.0"

[tool.poetry.group.dev.dependencies]
ruff = "^0.3.5"
pytest = "^8.1.1"


[build-system]
requires = ["poetry-core>=1.2.0"]
